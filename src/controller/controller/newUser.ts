import { Usuario } from "@/types/tipos";
import { crearHistorial } from "../CRUD/historialUsuario";
import { crearRegistroHistorial } from "../CRUD/historialEstadosUsuario";

export const crearUsuario = (usuario: Usuario) => {
    
}